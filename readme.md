# Desviación Estándar en Python

Este repositorio implementa el cálculo de la **desviación estándar** en Python utilizando la metodología **TDD (Test-Driven Development)**.

## 🧪 TDD (Test-Driven Development)

El desarrollo se realizó siguiendo el ciclo de TDD:

1. Escribir primero las **pruebas unitarias** que definen el comportamiento esperado.
2. Implementar la lógica mínima para que las pruebas pasen.
3. Refactorizar el código manteniendo todas las pruebas exitosas.

Las pruebas actúan como especificación y garantizan la corrección del cálculo.

## 📁 Estructura

```text
src/        # Lógica del cálculo
tests/      # Pruebas unitarias
```
Las pruebas se ejecutan con unittest usando el siguiente comando:

python -m unittest -v tests/Testdatos.py
